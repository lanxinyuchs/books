#%Module1.0
#	$Id$
#	$Source$

proc ModulesDisplay {} {
	puts stderr ""
	puts stderr "OTP 19.3.1"
}


proc ModulesHelp {} {
	puts stderr ""
	puts stderr "OTP 19.3.1"
}

#  Select path depending on OS flavour
if { [file exists /etc/redhat-release]} {
    set top /proj/5G_rcs/cache/OTP/R2A22/OTP_CXC2010679/native/x86_64
} else {
    set top /proj/5G_rcs/cache/OTP/R2A22/OTP_CXC2010679/native/x86_64
}

# 
set erlinterface [glob $top/lib/erl_interface-*]

prepend-path PATH	$top/bin:$erlinterface/bin
append-path MANPATH    	$top/man

setenv OTP_ROOT 	$top
setenv OTP		$top/lib/erlang

# Needed for "crypt" functions in "erlc", ...
module load openssl/1.0.0

#
# History of changes
# $Log$
#
# Emacs support
# Local Variables:
# mode:tcl
# End:
